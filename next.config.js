/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image9.coupangcdn.com', 'image10.coupangcdn.com'],
  },
};

module.exports = nextConfig;
